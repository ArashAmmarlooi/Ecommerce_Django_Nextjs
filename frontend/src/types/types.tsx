
export interface Category {
    category: any
    error: String
    isLoading: Boolean
}

export interface SubCategory {
    subcategory: any
    error: String
    isLoading: Boolean
}

export interface Product {
    product: any
    error: String
    isLoading: Boolean
}

export interface Cart {
    cart: any
    error: String
    isLoading: Boolean
}